## Options ##

# Prefix
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Reduce escape delay
set -g escape-time 10

# Enable focus events
set -g focus-events on

# Increase history limit
set -g history-limit 10000

# Disable mouse
set -g mouse off

# Vi-like copy mode
setw -g mode-keys vi

# Proper colors
set -s terminal-features "$TERM:RGB"
set -g default-terminal "tmux-256color"

## Keybindings ##

# Ctrl-Enter and Shift-Enter bindings
bind -n S-Enter send-keys Escape "[13;2u"
bind -n C-Enter send-keys Escape "[13;5u"

# Reload config
bind -n M-r source-file ~/.config/tmux/tmux.conf \; \
            display "Config reloaded!"

# Sessions list sorted by name
bind -n M-s choose-tree -sZ -O name

# Select window
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9
bind -n M-0 select-window -t 10

# Jump to last tab
bind -n M-Tab last-window

# Select panes
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Resize panes
bind -n M-H resize-pane -L 3
bind -n M-J resize-pane -D 2
bind -n M-K resize-pane -U 2
bind -n M-L resize-pane -R 3

# Zoom current pane
bind -n M-z resize-pane -Z

# Split window
bind -n M-C-h split-window -hb
bind -n M-C-j split-window -v
bind -n M-C-k split-window -vb
bind -n M-C-l split-window -h

# Move panes
bind -n M-f swap-pane -D
bind -n M-b swap-pane -U

# Kill panes, windows, sessions
bind -n M-Enter new-window
bind -n M-x kill-pane
bind -n M-q kill-window
bind -n M-Q confirm-before -p "Kill session? (y/n)" kill-session
bind -n M-d detach

# Command prompt
bind -n M-: command-prompt

# Enter copy mode
bind -n M-/ copy-mode

# Begin and copy selection
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel \
    "wl-copy || xclip -in -sel clipboard"

# Paste
bind -n M-p paste-buffer

## Theme ##

# Palette
set -g @color_default "#5a6378"
set -g @color_accent "#e6b450"
set -g @color_status "#70bf56"
set -g @color_window "#73b8ff"
set -g @color_pane "#1b1f29"

# Gitmux config path
set -g @gitmux_cfg ~/.config/gitmux/config.yml

# Start windows and panes numbering from 1
set -g base-index 1
setw -g pane-base-index 1

# Status on top
set -g status-position top

# Status styles
set -g status-style "fg=#{@color_default}"
set -g message-style "fg=#{@color_accent},underscore"

# Command prompt cursor
set -g prompt-cursor-style bar
set -g prompt-cursor-colour default

# Status lengths
set -g status-left-length 20
set -g status-right-length 50

# Status formats
set -g status-left "#[fg=#{@color_status},bold] #S#[default] "
set -g status-right "#(gitmux -cfg #{@gitmux_cfg} #{pane_current_path})"

# Windows
setw -g window-status-current-format "#[fg=#{@color_window},underscore]#{?window_zoomed_flag,#[italics],}#I:#W"
setw -g window-status-format "#[fg=#{@color_default}]#I:#W"

# Pane borders
set -g pane-active-border-style "fg=#{@color_accent}"
set -g pane-border-style "fg=#{@color_pane}"

## Tpm and plugins ##

# Custom plugins directory
set -g @plugins_dir ~/.local/share/tmux/plugins
setenv -gF TMUX_PLUGIN_MANAGER_PATH "#{@plugins_dir}"

# Plugins
set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-resurrect" 
set -g @plugin "tmux-plugins/tmux-continuum"
set -g @plugin "tmux-plugins/tmux-yank"

# Plugins options
set -g @resurrect-capture-pane-contents "on"
set -g @resurrect-dir "~/.local/state/tmux/resurrect"
set -g @continuum-restore "on"
set -g @continuum-save-interval "10"

# TPM local directory and remote repo url
set -gF @tpm_dir "#{@plugins_dir}/tpm"
set -g @tpm_repo "https://github.com/tmux-plugins/tpm"

# Automatically install TPM if it is not
if "test ! -d #{@tpm_dir}" \
   "run 'git clone #{@tpm_repo} #{@tpm_dir}' && \
         #{@tpm_dir}/bin/install_plugins && \
         echo \"Tpm successfully installed!\""

# Run plugin manager
run "#{@tpm_dir}/tpm"
